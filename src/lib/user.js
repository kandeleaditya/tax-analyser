const test_username = 'aditya';
const test_password = 'aditya';

function createInternalUser(username, password) {
  const hashedPassword = hashUserPassword(password);
}
export default createInternalUser;
